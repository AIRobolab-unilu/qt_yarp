# Copyright: (C) 2010 RobotCub Consortium
# Author: Lorenzo Natale
# CopyPolicy: Released under the terms of the GNU GPL v2.0.

if (NOT CUDDIE_FOUND)

message(STATUS "Using Cuddie from install")

#set(CUDDIE_INCLUDE_DIRS "@CUDDIE_INCLUDE_DIRS@" CACHE INTERNAL "Include directories needed for Cuddie")
set(CUDDIE_MODULE_PATH "@CUDDIE_MODULE_PATH@" CACHE INTERNAL "Cuddie cmake scripts directory")
set(CUDDIE_LIBRARIES "@CUDDIE_TARGETS@" CACHE INTERNAL "List of Cuddie libraries")
set(CUDDIE_LINK_FLAGS "@CUDDIE_LINK_FLAGS@" CACHE INTERNAL "List of Cuddie linker options")
set(CUDDIE_LINK_DIRECTORIES "@CUDDIE_LINK_DIRECTORIES@")
set(CUDDIE_INSTALL_PREFIX "@CMAKE_INSTALL_PREFIX@")

set(CUDDIE_PLUGIN_MANIFESTS_INSTALL_DIR "@CUDDIE_PLUGIN_MANIFESTS_INSTALL_DIR@")
set(CUDDIE_MODULES_INSTALL_DIR "@CUDDIE_MODULES_INSTALL_DIR@")
set(CUDDIE_APPLICATIONS_INSTALL_DIR "@CUDDIE_APPLICATIONS_INSTALL_DIR@")
set(CUDDIE_TEMPLATES_INSTALL_DIR "@CUDDIE_TEMPLATES_INSTALL_DIR@")
set(CUDDIE_APPLICATIONS_TEMPLATES_INSTALL_DIR "@CUDDIE_APPLICATIONS_TEMPLATES_INSTALL_DIR@")
set(CUDDIE_MODULES_TEMPLATES_INSTALL_DIR "@CUDDIE_MODULES_TEMPLATES_INSTALL_DIR@")
set(CUDDIE_CONTEXTS_INSTALL_DIR "@CUDDIE_CONTEXTS_INSTALL_DIR@")

include("@CMAKE_INSTALL_PREFIX@/lib/CUDDIE/cuddie-export-install.cmake")
include("@CMAKE_INSTALL_PREFIX@/lib/CUDDIE/cuddie-export-install-includes.cmake")

# This is not ideal and should be removed. At the moment
# only needed by OpenCV
link_directories(${CUDDIE_LINK_DIRECTORIES})

set (CUDDIE_FOUND TRUE)
endif (NOT CUDDIE_FOUND)
